# ---- 构建产物与依赖：不进仓库 ----
# 前端
ui/node_modules/
ui/dist/
ui/dist-electron/
ui/release/
ui/.vite/
# 后端（由 scripts/stage-backend.sh 生成，随 AppImage 一起打包）
ui/backend/
# C++ 后端
build/
.cache/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
.DS_Store

# ---- 可能的本地密钥 / 凭据 ----
*.pem
*.key
id_rsa*
.env
.env.*
